# GENERATED by model_sketch -- DO NOT EDIT.

eigs_lanczos(
    ops::OpSum,
    block::Block;
    neigvals::Int64 = 1,
    precision::Float64 = 1e-12,
    max_iterations::Int64 = 1000,
    deflation_tol::Float64 = 1e-7,
    random_seed::Int64 = 42,
) = convert(
    EigsLanczosResult,
    fun_eigs_lanczos(
        ops.cxx_object,
        block.cxx_object,
        neigvals,
        precision,
        max_iterations,
        deflation_tol,
        random_seed,
    ),
)
eigs_lanczos(
    ops::CSRMatrix,
    block::Block;
    neigvals::Int64 = 1,
    precision::Float64 = 1e-12,
    max_iterations::Int64 = 1000,
    deflation_tol::Float64 = 1e-7,
    random_seed::Int64 = 42,
) = GC.@preserve ops (convert(
    EigsLanczosResult,
    fun_eigs_lanczos(
        csr_view(ops),
        block.cxx_object,
        neigvals,
        precision,
        max_iterations,
        deflation_tol,
        random_seed,
    ),
))
eigs_lanczos(
    ops::OpSum,
    state0::State;
    neigvals::Int64 = 1,
    precision::Float64 = 1e-12,
    max_iterations::Int64 = 1000,
    deflation_tol::Float64 = 1e-7,
) = convert(
    EigsLanczosResult,
    fun_eigs_lanczos(
        ops.cxx_object,
        state0.cxx_object,
        neigvals,
        precision,
        max_iterations,
        deflation_tol,
    ),
)
eigs_lanczos(
    ops::CSRMatrix,
    state0::State;
    neigvals::Int64 = 1,
    precision::Float64 = 1e-12,
    max_iterations::Int64 = 1000,
    deflation_tol::Float64 = 1e-7,
) = GC.@preserve ops (convert(
    EigsLanczosResult,
    fun_eigs_lanczos(
        csr_view(ops),
        state0.cxx_object,
        neigvals,
        precision,
        max_iterations,
        deflation_tol,
    ),
))
eigvals_lanczos(
    ops::OpSum,
    block::Block;
    neigvals::Int64 = 1,
    precision::Float64 = 1e-12,
    max_iterations::Int64 = 1000,
    deflation_tol::Float64 = 1e-7,
    random_seed::Int64 = 42,
) = convert(
    EigvalsLanczosResult,
    fun_eigvals_lanczos(
        ops.cxx_object,
        block.cxx_object,
        neigvals,
        precision,
        max_iterations,
        deflation_tol,
        random_seed,
    ),
)
eigvals_lanczos(
    A::CSRMatrix,
    block::Block;
    neigvals::Int64 = 1,
    precision::Float64 = 1e-12,
    max_iterations::Int64 = 1000,
    deflation_tol::Float64 = 1e-7,
    random_seed::Int64 = 42,
) = GC.@preserve A (convert(
    EigvalsLanczosResult,
    fun_eigvals_lanczos(
        csr_view(A),
        block.cxx_object,
        neigvals,
        precision,
        max_iterations,
        deflation_tol,
        random_seed,
    ),
))
eigvals_lanczos(
    ops::OpSum,
    psi0::State;
    neigvals::Int64 = 1,
    precision::Float64 = 1e-12,
    max_iterations::Int64 = 1000,
    deflation_tol::Float64 = 1e-7,
) = convert(
    EigvalsLanczosResult,
    fun_eigvals_lanczos(
        ops.cxx_object,
        psi0.cxx_object,
        neigvals,
        precision,
        max_iterations,
        deflation_tol,
    ),
)
eigvals_lanczos(
    A::CSRMatrix,
    psi0::State;
    neigvals::Int64 = 1,
    precision::Float64 = 1e-12,
    max_iterations::Int64 = 1000,
    deflation_tol::Float64 = 1e-7,
) = GC.@preserve A (convert(
    EigvalsLanczosResult,
    fun_eigvals_lanczos(
        csr_view(A),
        psi0.cxx_object,
        neigvals,
        precision,
        max_iterations,
        deflation_tol,
    ),
))
eigvals_lanczos_inplace(
    ops::OpSum,
    psi0::State;
    neigvals::Int64 = 1,
    precision::Float64 = 1e-12,
    max_iterations::Int64 = 1000,
    deflation_tol::Float64 = 1e-7,
) = convert(
    EigvalsLanczosResult,
    fun_eigvals_lanczos_inplace(
        ops.cxx_object,
        psi0.cxx_object,
        neigvals,
        precision,
        max_iterations,
        deflation_tol,
    ),
)
eigvals_lanczos_inplace(
    A::CSRMatrix,
    psi0::State;
    neigvals::Int64 = 1,
    precision::Float64 = 1e-12,
    max_iterations::Int64 = 1000,
    deflation_tol::Float64 = 1e-7,
) = GC.@preserve A (convert(
    EigvalsLanczosResult,
    fun_eigvals_lanczos_inplace(
        csr_view(A),
        psi0.cxx_object,
        neigvals,
        precision,
        max_iterations,
        deflation_tol,
    ),
))
eigs_lobpcg(
    ops::OpSum,
    block::Block;
    neigs::Int64 = 1,
    guard::Int64 = 2,
    tol::Float64 = 1e-10,
    max_iterations::Int64 = 1000,
    random_seed::Int64 = 42,
) = convert(
    EigsLobpcgResult,
    fun_eigs_lobpcg(
        ops.cxx_object,
        block.cxx_object,
        neigs,
        guard,
        tol,
        max_iterations,
        random_seed,
    ),
)
eigs_lobpcg(
    A::CSRMatrix,
    block::Block;
    neigs::Int64 = 1,
    guard::Int64 = 2,
    tol::Float64 = 1e-10,
    max_iterations::Int64 = 1000,
    random_seed::Int64 = 42,
) = GC.@preserve A (convert(
    EigsLobpcgResult,
    fun_eigs_lobpcg(
        csr_view(A),
        block.cxx_object,
        neigs,
        guard,
        tol,
        max_iterations,
        random_seed,
    ),
))
eigval0(
    ops::OpSum,
    block::Block;
    precision::Float64 = 1e-12,
    max_iterations::Int64 = 1000,
    random_seed::Int64 = 42,
) = fun_eigval0(ops.cxx_object, block.cxx_object, precision, max_iterations, random_seed)
eigval0(
    ops::CSRMatrix,
    block::Block;
    precision::Float64 = 1e-12,
    max_iterations::Int64 = 1000,
    random_seed::Int64 = 42,
) = GC.@preserve ops (fun_eigval0(
    csr_view(ops),
    block.cxx_object,
    precision,
    max_iterations,
    random_seed,
))
eig0(
    ops::OpSum,
    block::Block;
    precision::Float64 = 1e-12,
    max_iterations::Int64 = 1000,
    random_seed::Int64 = 42,
) = (
    r = fun_eig0(ops.cxx_object, block.cxx_object, precision, max_iterations, random_seed);
    (r[1], State(r[2]))
)
eig0(
    ops::CSRMatrix,
    block::Block;
    precision::Float64 = 1e-12,
    max_iterations::Int64 = 1000,
    random_seed::Int64 = 42,
) = GC.@preserve ops (
    (r = fun_eig0(csr_view(ops), block.cxx_object, precision, max_iterations, random_seed);
    (r[1], State(r[2])))
)
eigvals(
    ops::OpSum,
    block::Block,
    neigs::Int64;
    precision::Float64 = 1e-12,
    max_iterations::Int64 = 1000,
    random_seed::Int64 = 42,
) = to_julia(
    fun_eigvals(
        ops.cxx_object,
        block.cxx_object,
        neigs,
        precision,
        max_iterations,
        random_seed,
    ),
)
eigvals(
    ops::CSRMatrix,
    block::Block,
    neigs::Int64;
    precision::Float64 = 1e-12,
    max_iterations::Int64 = 1000,
    random_seed::Int64 = 42,
) = GC.@preserve ops (to_julia(
    fun_eigvals(
        csr_view(ops),
        block.cxx_object,
        neigs,
        precision,
        max_iterations,
        random_seed,
    ),
))
eigs(
    ops::OpSum,
    block::Block,
    neigs::Int64;
    precision::Float64 = 1e-12,
    max_iterations::Int64 = 1000,
    random_seed::Int64 = 42,
) = (
    r = fun_eigs(
        ops.cxx_object,
        block.cxx_object,
        neigs,
        precision,
        max_iterations,
        random_seed,
    );
    (to_julia(r[1]), State(r[2]))
)
eigs(
    ops::CSRMatrix,
    block::Block,
    neigs::Int64;
    precision::Float64 = 1e-12,
    max_iterations::Int64 = 1000,
    random_seed::Int64 = 42,
) = GC.@preserve ops (
    (r = fun_eigs(
        csr_view(ops),
        block.cxx_object,
        neigs,
        precision,
        max_iterations,
        random_seed,
    );
    (to_julia(r[1]), State(r[2])))
)
evolve_lanczos(
    H::OpSum,
    psi::State,
    tau::Float64;
    precision::Float64 = 1e-12,
    shift::Float64 = 0.0,
    normalize::Bool = false,
    max_iterations::Int64 = 1000,
    deflation_tol::Float64 = 1e-7,
) = convert(
    EvolveLanczosResult,
    fun_evolve_lanczos(
        H.cxx_object,
        psi.cxx_object,
        tau,
        precision,
        shift,
        normalize,
        max_iterations,
        deflation_tol,
    ),
)
evolve_lanczos(
    H::OpSum,
    psi::State,
    tau::ComplexF64;
    precision::Float64 = 1e-12,
    shift::Float64 = 0.0,
    normalize::Bool = false,
    max_iterations::Int64 = 1000,
    deflation_tol::Float64 = 1e-7,
) = convert(
    EvolveLanczosResult,
    fun_evolve_lanczos(
        H.cxx_object,
        psi.cxx_object,
        tau,
        precision,
        shift,
        normalize,
        max_iterations,
        deflation_tol,
    ),
)
evolve_lanczos(
    H::CSRMatrix,
    psi::State,
    tau::Float64;
    precision::Float64 = 1e-12,
    shift::Float64 = 0.0,
    normalize::Bool = false,
    max_iterations::Int64 = 1000,
    deflation_tol::Float64 = 1e-7,
) = GC.@preserve H (convert(
    EvolveLanczosResult,
    fun_evolve_lanczos(
        csr_view(H),
        psi.cxx_object,
        tau,
        precision,
        shift,
        normalize,
        max_iterations,
        deflation_tol,
    ),
))
evolve_lanczos(
    H::CSRMatrix,
    psi::State,
    tau::ComplexF64;
    precision::Float64 = 1e-12,
    shift::Float64 = 0.0,
    normalize::Bool = false,
    max_iterations::Int64 = 1000,
    deflation_tol::Float64 = 1e-7,
) = GC.@preserve H (convert(
    EvolveLanczosResult,
    fun_evolve_lanczos(
        csr_view(H),
        psi.cxx_object,
        tau,
        precision,
        shift,
        normalize,
        max_iterations,
        deflation_tol,
    ),
))
evolve_lanczos_inplace(
    H::OpSum,
    psi::State,
    tau::Float64;
    precision::Float64 = 1e-12,
    shift::Float64 = 0.0,
    normalize::Bool = false,
    max_iterations::Int64 = 1000,
    deflation_tol::Float64 = 1e-7,
) = convert(
    EvolveLanczosInplaceResult,
    fun_evolve_lanczos_inplace(
        H.cxx_object,
        psi.cxx_object,
        tau,
        precision,
        shift,
        normalize,
        max_iterations,
        deflation_tol,
    ),
)
evolve_lanczos_inplace(
    H::OpSum,
    psi::State,
    tau::ComplexF64;
    precision::Float64 = 1e-12,
    shift::Float64 = 0.0,
    normalize::Bool = false,
    max_iterations::Int64 = 1000,
    deflation_tol::Float64 = 1e-7,
) = convert(
    EvolveLanczosInplaceResult,
    fun_evolve_lanczos_inplace(
        H.cxx_object,
        psi.cxx_object,
        tau,
        precision,
        shift,
        normalize,
        max_iterations,
        deflation_tol,
    ),
)
evolve_lanczos_inplace(
    H::CSRMatrix,
    psi::State,
    tau::Float64;
    precision::Float64 = 1e-12,
    shift::Float64 = 0.0,
    normalize::Bool = false,
    max_iterations::Int64 = 1000,
    deflation_tol::Float64 = 1e-7,
) = GC.@preserve H (convert(
    EvolveLanczosInplaceResult,
    fun_evolve_lanczos_inplace(
        csr_view(H),
        psi.cxx_object,
        tau,
        precision,
        shift,
        normalize,
        max_iterations,
        deflation_tol,
    ),
))
evolve_lanczos_inplace(
    H::CSRMatrix,
    psi::State,
    tau::ComplexF64;
    precision::Float64 = 1e-12,
    shift::Float64 = 0.0,
    normalize::Bool = false,
    max_iterations::Int64 = 1000,
    deflation_tol::Float64 = 1e-7,
) = GC.@preserve H (convert(
    EvolveLanczosInplaceResult,
    fun_evolve_lanczos_inplace(
        csr_view(H),
        psi.cxx_object,
        tau,
        precision,
        shift,
        normalize,
        max_iterations,
        deflation_tol,
    ),
))
imaginary_time_evolve(
    H::OpSum,
    psi::State,
    time::Float64;
    precision::Float64 = 1e-12,
    shift::Float64 = 0.0,
) = State(fun_imaginary_time_evolve(H.cxx_object, psi.cxx_object, time, precision, shift))
imaginary_time_evolve(
    H::CSRMatrix,
    psi::State,
    time::Float64;
    precision::Float64 = 1e-12,
    shift::Float64 = 0.0,
) = GC.@preserve H (State(
    fun_imaginary_time_evolve(csr_view(H), psi.cxx_object, time, precision, shift),
))
imaginary_time_evolve_inplace(
    H::OpSum,
    psi::State,
    time::Float64;
    precision::Float64 = 1e-12,
    shift::Float64 = 0.0,
) = fun_imaginary_time_evolve_inplace(H.cxx_object, psi.cxx_object, time, precision, shift)
imaginary_time_evolve_inplace(
    H::CSRMatrix,
    psi::State,
    time::Float64;
    precision::Float64 = 1e-12,
    shift::Float64 = 0.0,
) = GC.@preserve H (fun_imaginary_time_evolve_inplace(
    csr_view(H),
    psi.cxx_object,
    time,
    precision,
    shift,
))
time_evolve_expokit(
    H::OpSum,
    psi0::State,
    time::Float64;
    precision::Float64 = 1e-12,
    m::Int64 = 30,
    anorm::Float64 = 0.0,
    nnorm::Int64 = 2,
) = convert(
    TimeEvolveExpokitResult,
    fun_time_evolve_expokit(
        H.cxx_object,
        psi0.cxx_object,
        time,
        precision,
        m,
        anorm,
        nnorm,
    ),
)
time_evolve_expokit(
    H::CSRMatrix,
    psi0::State,
    time::Float64;
    precision::Float64 = 1e-12,
    m::Int64 = 30,
    anorm::Float64 = 0.0,
    nnorm::Int64 = 2,
) = GC.@preserve H (convert(
    TimeEvolveExpokitResult,
    fun_time_evolve_expokit(csr_view(H), psi0.cxx_object, time, precision, m, anorm, nnorm),
))
time_evolve_expokit_inplace(
    H::OpSum,
    psi::State,
    time::Float64;
    precision::Float64 = 1e-12,
    m::Int64 = 30,
    anorm::Float64 = 0.0,
    nnorm::Int64 = 2,
) = convert(
    TimeEvolveExpokitInplaceResult,
    fun_time_evolve_expokit_inplace(
        H.cxx_object,
        psi.cxx_object,
        time,
        precision,
        m,
        anorm,
        nnorm,
    ),
)
time_evolve_expokit_inplace(
    H::CSRMatrix,
    psi::State,
    time::Float64;
    precision::Float64 = 1e-12,
    m::Int64 = 30,
    anorm::Float64 = 0.0,
    nnorm::Int64 = 2,
) = GC.@preserve H (convert(
    TimeEvolveExpokitInplaceResult,
    fun_time_evolve_expokit_inplace(
        csr_view(H),
        psi.cxx_object,
        time,
        precision,
        m,
        anorm,
        nnorm,
    ),
))
time_evolve(
    H::OpSum,
    psi::State,
    time::Float64;
    precision::Float64 = 1e-12,
    algorithm::String = "lanczos",
) = State(fun_time_evolve(H.cxx_object, psi.cxx_object, time, precision, algorithm))
time_evolve(
    H::CSRMatrix,
    psi::State,
    time::Float64;
    precision::Float64 = 1e-12,
    algorithm::String = "lanczos",
) = GC.@preserve H (State(
    fun_time_evolve(csr_view(H), psi.cxx_object, time, precision, algorithm),
))
time_evolve_inplace(
    H::OpSum,
    psi::State,
    time::Float64;
    precision::Float64 = 1e-12,
    algorithm::String = "lanczos",
) = fun_time_evolve_inplace(H.cxx_object, psi.cxx_object, time, precision, algorithm)
time_evolve_inplace(
    H::CSRMatrix,
    psi::State,
    time::Float64;
    precision::Float64 = 1e-12,
    algorithm::String = "lanczos",
) = GC.@preserve H (fun_time_evolve_inplace(
    csr_view(H),
    psi.cxx_object,
    time,
    precision,
    algorithm,
))
